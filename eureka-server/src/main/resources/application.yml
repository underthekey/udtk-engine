server:
  port: ${EUREKA_SERVER_PORT}

user-details:
  username: ${USERNAME}
  password: ${PASSWORD}
  role: ${ROLE}

spring:
  profiles:
    active: dev
    include: local
  application:
    name: udtk-eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false